
extends ../_layout_moodle.pug
block variables
  - var title = '英語のみ受講｜ダッシュボード | stg'  // 変数の定義
  - var calender = 'events-none' //カレンダーなし
  // SubjectIds 配列の定義
  - var SubjectIds = env === 'stg' ? [229] : env === 'lmswaomirai' ? [1005] : []
block contents
  include _parts-main-dashboard.pug
