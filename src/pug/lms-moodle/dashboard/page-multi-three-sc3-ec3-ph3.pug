
extends ../_layout_moodle.pug
block variables
  - var title = 'ダッシュボード'  // 変数の定義
  - var calender = 'events-include'
  // SubjectIds 配列の定義
  - var SubjectIds = env === 'stg' ? [229,242,244,246] : env === 'lmswaomirai' ? [271,258,268,263] : []
block contents
  include _parts-main-dashboard.pug
