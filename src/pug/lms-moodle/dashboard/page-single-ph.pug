extends ../_layout_moodle.pug
block variables
  - var title = 'ダッシュボード'  // 変数の定義
  - var status = 'purchased'
  - var calender = 'events-none' //カレンダーなし
  - var SubjectIds = env === 'stg' ? [212] : env === 'lmswaomirai' ? [255] : []
block contents
  include _parts-main-dashboard.pug
