extends ../_layout_moodle.pug
block contents
  - var envClasses = env === 'stg' ? ['course-221'] : env === 'lms' ? [] : ['course-256']
  - var title = '哲学レベル1'  // 変数の定義
  body#page-mod-questionnaire-view(
    class=[
      'format-flexsections',
      'path-mod',
      'path-mod-questionnaire',
      'chrome',
      'dir-ltr',
      ...envClasses
    ].join(' ')
  )
    include parts-page-path-mod-questionnaire-detail.pug